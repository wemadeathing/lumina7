---
export interface Props {
  variant?: 'default' | 'hover' | 'bordered' | 'flat';
  padding?: 'none' | 'sm' | 'md' | 'lg';
  class?: string;
  href?: string;
  target?: string;
  rel?: string;
}

const { 
  variant = 'default', 
  padding = 'md', 
  class: className = '',
  href,
  target,
  rel,
  ...rest 
} = Astro.props;

const baseClasses = 'bg-white dark:bg-gray-800 overflow-hidden transition-all duration-300';

const variants = {
  default: 'rounded-[4px] shadow-sm dark:shadow-gray-900/50',
  hover: 'rounded-[4px] shadow-sm dark:shadow-gray-900/50 hover:shadow-lg dark:hover:shadow-gray-900/70 hover:-translate-y-1 cursor-pointer',
  bordered: 'rounded-[4px] border border-gray-200 dark:border-gray-700 hover:border-gray-300 dark:hover:border-gray-600',
  flat: 'rounded-[4px]'
};

const paddings = {
  none: '',
  sm: 'p-4',
  md: 'p-6',
  lg: 'p-8'
};

const classes = `${baseClasses} ${variants[variant]} ${paddings[padding]} ${className}`;

const Element = href ? 'a' : 'div';
---

<Element 
  class={classes}
  href={href}
  target={target}
  rel={rel}
  {...rest}
>
  <slot />
</Element>
